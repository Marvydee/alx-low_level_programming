#include "main.h"

/**
 * _strchr - locates a character in a string
 * @s: pointer
 * @c: character to be located
 * Return: return null
 */
char *_strchr(char *s, char c)
{
	while (*s != '\0' && *s == 'c')
	{
		return (s);
		s++;
	}
	return ('\0');
}
